# tetsuo-ai/homebrew-agenc

Homebrew tap for the [AgenC CLI](https://github.com/tetsuo-ai/agenc-releases).

```bash
brew tap tetsuo-ai/agenc
brew install agenc
```

The formula wraps the same sha256-verified installer as
`curl -fsSL https://get.agenc.ag/install.sh | sh`.
